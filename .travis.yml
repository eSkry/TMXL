language: cpp
compiler: gcc

os: lunux
addons:
  apt:
    sources:
      - ubuntu-toolchan-r-test
    packages:
     - g++
     - gcc
     - cmake

script:
  - export CC=gcc
  - export CXX=g++
  - cmake --version
  - cmake CMakeLists.txt
  - cmake --build .
